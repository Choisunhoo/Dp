# baekjoon 2133 타일 채우기

# 알고싶은 3 * N 크기의 벽을 입력 
N = int(input())

# dp 테이블 생성및 0번째 초기화
dp = [0] * 31
dp[0] = 1

# 반복문을 통해서 dp 테이블을 채워줌 
# 3 * N 을 2*1 혹은 1*2 로 채우는 것은 짝수만 채울수 있다는 것과 같음 
for i in range(2, N+1, 2):
    # i 값보다 작은 제일큰 짝수에 값을 3곱해서 저장 
    dp[i] = dp[i-2] * 3
    # 그 외의 것은 i-2값보다 적은 짝수의 값을 2곱해서 더해줌
    for j in range(0, i-2, 2):
        dp[i] += dp[j] * 2

# 결과 출력 
print(dp[N])

# 문제 1로 만들기

# 정수 X 값 입력 받기
X = int(input())

# 앞에서 계산된 결과를 정하기 위한 DP 테이블 초기화 DP 테이블은 X의 최대값인 30000 보다 1개더 많게
dp = [0] * 30001

# 다이나믹 프로그래밍 진행(바텀업 방식으로)
for i in range(2, X+1):
    #현재의 수에서 1을 빼는 경우
    dp[i] = dp[i-1] +1
    # 현재의 수가 5로 나누어 떨어지는 경우
    if i % 5 == 0:
        dp[i] = min(dp[i], dp[i//5]+1)
    # 현재의 수가 3로 나누어 떨어지는 경우
    if i % 3 == 0:
        dp[i] = min(dp[i], dp[i//3]+1)
    # 현재의 수가 2로 나누어 떨어지는 경우
    if i % 2 == 0:
        dp[i] = min(dp[i], dp[i//2]+1)
  

print(dp[X])
